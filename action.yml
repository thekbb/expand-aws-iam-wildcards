name: 'Expand AWS IAM Wildcards'
description: 'Helps reviewers by expanding IAM wildcards in changesets via inline PR comments linking to AWS docs.'
author: 'Kevin Behrens <kevin@thekbb.net>'

branding:
  icon: 'eye'
  color: 'blue'

inputs:
  github-token:
    description: 'GitHub token for API access'
    required: false
    default: ${{ github.token }}
  file-patterns:
    description: 'Glob patterns for files to scan (comma-separated)'
    required: false
    default: '**/*.json,**/*.yaml,**/*.yml,**/*.tf,**/*.ts,**/*.js'
  collapse-threshold:
    description: 'Number of expanded actions before collapsing into details element'
    required: false
    default: '5'

runs:
  using: 'node20'
  main: 'dist/index.js'
